{{- if .Values.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "navidrome.fullname" . }}-config
  labels:
    {{- include "navidrome.labels" . | nindent 4 }}
data:
  navidrome.toml: |
    {{- .Values.config.content | trim | nindent 4 }}
{{- end }}
